<div class='benefit-card'>
    <div class='benefit-card__header'>
        <div class='benefit-card__logo' style="background: url({{backgroundURL}});">
        </div>
        <h3 class='benefit-card__title'>{{headerTitle}}</h3>
    </div>
    <ul class='benefit-card__desctiption'>
        {{#each benefits}}
        <li>{{this}}</li>
        {{/each}}
        <a href='{{hrefURL}}'>Learn more</a>
    </ul>

</div>